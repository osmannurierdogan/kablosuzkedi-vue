/* export default {
  namespaced: true,
  state: {
    itemList: [
      {
        id: new Date().getTime(),
        title: "Task #1",
      },
    ],
    userList: [
      {
        id: new Date().getTime(),
        name: "User #1",
      },
    ],
  },
  mutations: {
    _setItem(state, item) {
      state.itemList.push(item);
    },
    _setUser(state, user) {
      state.userList.push(user);
    },
  },
  getters: {
    _userList(state) {
      return state.userList;
    },
    _itemList(state) {
      return state.itemList;
    },
  },
};
 */
